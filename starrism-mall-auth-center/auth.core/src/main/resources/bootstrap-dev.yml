spring:
  cloud:
    nacos:
      discovery:
        server-addr: http://localhost:8848
        namespace: DEV
      config:
        server-addr: http://localhost:8848
        file-extension: yaml
        namespace: DEV